// Placeholder only — NOT the Stitch "Jadwal Kajian" design. Exists purely so
// the bottom navigation's highlighted center tab has somewhere to route to
// while this increment only covers Home. Will be replaced entirely in the
// Kajian increment.
export default function Kajian() {
  return (
    <div className="px-md py-3xl text-center text-on-surface-variant font-body-sm">
      Kajian — coming in a future increment
    </div>
  )
}
